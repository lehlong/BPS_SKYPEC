
@model SMO.Service.MD.RouteService
@{
    Model.ViewId = Guid.NewGuid().ToString();
    Model.FormId = Guid.NewGuid().ToString();
}
<div id='@Model.ViewId' class="container-fluid child-content">
    @using (Ajax.BeginForm("Create", null, FormDataUtils.GetAjaxOptions(Model.ViewId), new { @id = Model.FormId }))
    {
        @Html.AntiForgeryToken()
        <div class="card border-grey">
            <div class="body" style="padding-top: 0px;">
                <ul class="nav nav-tabs" role="tablist">
                    <li role="presentation" class="active">
                        <a style="padding-left:0px"><i class="material-icons col-fecon">clear_all</i> TẠO MỚI SÂN BAY</a>
                    </li>
                </ul>

                <div class="tab-content">
                    <div class="page-function">
                        <div class="page-function-button m-r-20">
                            @Html.MyButton("cmdBack", "Quay lại", string.Format("Forms.Close('{0}');", @Model.ViewId), "Alt+N", "arrow_back")
                            @Html.MyButton("cmdSave", "Lưu lại", string.Format("Forms.SubmitForm('{0}');", @Model.FormId), "Alt+N", "done_all")
                        </div>
                    </div>

                    <div class="row m-t-20">
                        <div class="col-xs-1"></div>
                        <div class="col-lg-7 col-xs-10">
                            <label>Mã tuyến đường</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.CODE)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.CODE)
                            </div>

                            <label>Tên tuyến đường</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.NAME)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.NAME)
                            </div>

                            <label>Điểm đầu</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.FIRST_POINT)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.FIRST_POINT)
                            </div>

                            <label>Điểm cuối</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.FINAL_POINT)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.FINAL_POINT)
                            </div>

                            <label>Km có hàng</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.KM_CO_HANG)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.KM_CO_HANG)
                            </div>

                            <label>Km không hàng</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.KM_KHONG_HANG)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.KM_KHONG_HANG)
                            </div>

                            <label>Hệ số hao hụt</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.HE_SO_HAO_HUT)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.HE_SO_HAO_HUT)
                            </div>

                            <label>Parent Level ID</label>
                            <div class="form-group">
                                <div class="form-line">
                                    @Html.MyTextBoxFor(m => m.ObjDetail.PARENT_LEVEL_ID)
                                </div>
                                @Html.ValidationMessageFor(m => m.ObjDetail.PARENT_LEVEL_ID)
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    <script type="text/javascript">
        $(function () {
            Forms.CompleteUI();
        });
    </script>
</div>
